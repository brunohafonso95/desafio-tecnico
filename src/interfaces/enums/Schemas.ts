enum Schemas {
  ApplicationConfigSchema = 'ApplicationConfigSchema',
  AuthConfigSchema = 'AuthConfigSchema',
  MongoConfigSchema = 'MongoConfigSchema',
  UserSchema = 'UserSchema',
}

export default Schemas;
